<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.csf.database.mappers.TmpMemberMapper">
	<select id="selectAll" resultType="TmpMemberVO">
		SELECT *
		FROM MEMBERS
	</select>
	<select id="selectById" parameterType="int" resultType="TmpMemberVO">
		SELECT *
		FROM MEMBERS
		WHERE id = #{id}
	</select>
	<insert id="insert" parameterType="TmpMemberVO">
		INSERT INTO MEMBERS(id, name, job)
		VALUES(id_seq.nextval, #{name}, #{job})
	</insert>
	<update id="update" parameterType="TmpMemberVO">
		UPDATE MEMBERS
		SET name = #{name},
			job = #{job}
		WHERE id = #{id}
	</update>
	<delete id="delete" parameterType="int">
		DELETE MEMBERS
		WHERE id = #{id}
	</delete>
</mapper>